# Internationalization (i18n) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add four-language support (TR, EN, DE, AR) with locale-prefixed URLs, RTL layout for Arabic, UI translations via next-intl, and Shopify `@inContext` for localized product content.

**Architecture:** `app/[locale]/` routing with `proxy.ts` for locale detection and redirect. `next-intl` manages UI string translations with `fallbackLocale: 'en'`. `shopifyFetch` gains a `locale` param that injects `@inContext(language, country)` into every Shopify query. Arabic RTL is handled via `dir` attribute on `<html>` and Tailwind `rtl:` variants.

**Tech Stack:** next-intl, Next.js 16 App Router (`proxy.ts`), Tailwind CSS v4 RTL variants, Shopify Storefront API `@inContext`

> **Note:** Next.js 16 renamed `middleware.ts` → `proxy.ts` and `middleware()` → `proxy()`. Do NOT create a `middleware.ts` file.

---

## File Map

| File | Responsibility |
|------|---------------|
| `i18n/config.ts` | Locale list, default locale, locale→Shopify mapping |
| `i18n/request.ts` | next-intl `getRequestConfig` |
| `proxy.ts` | Locale detection, redirect to `/[locale]/...` |
| `messages/tr.json` | Turkish UI strings (source of truth) |
| `messages/en.json` | English UI strings |
| `messages/de.json` | German UI strings |
| `messages/ar.json` | Arabic UI strings |
| `app/[locale]/layout.tsx` | Root layout: `<html lang dir>`, fonts |
| `lib/shopify/client.ts` | Modify: add `locale` param → `@inContext` injection |
| `lib/shopify/types.ts` | Modify: add `locale` to `ShopifyFetchOptions` |
| `lib/shopify/client.test.ts` | Modify: add `@inContext` test |

---

## Task 1: Install next-intl

**Files:**
- Modify: `package.json`

- [ ] **Step 1: Install next-intl**

```bash
npm install next-intl
```

- [ ] **Step 2: Verify install**

```bash
npx tsc --noEmit
```

Expected: no errors.

- [ ] **Step 3: Commit**

```bash
git add package.json package-lock.json
git commit -m "feat: install next-intl"
```

---

## Task 2: i18n config

**Files:**
- Create: `i18n/config.ts`

- [ ] **Step 1: Create config**

```bash
mkdir -p i18n
```

Create `i18n/config.ts`:

```ts
export const locales = ['tr', 'en', 'de', 'ar'] as const
export type Locale = (typeof locales)[number]

export const defaultLocale: Locale = 'tr'

export const rtlLocales: Locale[] = ['ar']

export function isRtl(locale: Locale): boolean {
  return rtlLocales.includes(locale)
}

export const shopifyLocaleMap: Record<Locale, { language: string; country: string }> = {
  tr: { language: 'TR', country: 'TR' },
  en: { language: 'EN', country: 'US' },
  de: { language: 'DE', country: 'DE' },
  ar: { language: 'AR', country: 'SA' },
}
```

- [ ] **Step 2: Verify TypeScript**

```bash
npx tsc --noEmit
```

Expected: no errors.

- [ ] **Step 3: Commit**

```bash
git add i18n/config.ts
git commit -m "feat: add i18n locale config"
```

---

## Task 3: next-intl request config

**Files:**
- Create: `i18n/request.ts`

- [ ] **Step 1: Create request config**

Create `i18n/request.ts`:

```ts
import { getRequestConfig } from 'next-intl/server'
import { defaultLocale, locales, type Locale } from './config'

function deepMerge(base: Record<string, unknown>, override: Record<string, unknown>): Record<string, unknown> {
  const result = { ...base }
  for (const key in override) {
    const val = override[key]
    if (val && typeof val === 'object' && !Array.isArray(val)) {
      result[key] = deepMerge((base[key] as Record<string, unknown>) ?? {}, val as Record<string, unknown>)
    } else {
      result[key] = val
    }
  }
  return result
}

export default getRequestConfig(async ({ requestLocale }) => {
  let locale = await requestLocale
  if (!locale || !locales.includes(locale as Locale)) {
    locale = defaultLocale
  }

  const fallback = (await import(`../messages/en.json`)).default
  const localeMessages = locale !== 'en'
    ? (await import(`../messages/${locale}.json`)).default
    : fallback
  const messages = deepMerge(fallback, localeMessages)

  return {
    locale,
    messages,
    timeZone: 'Europe/Istanbul',
    now: new Date(),
  }
})
```

- [ ] **Step 2: Verify TypeScript**

```bash
npx tsc --noEmit
```

Expected: no errors.

- [ ] **Step 3: Commit**

```bash
git add i18n/request.ts
git commit -m "feat: add next-intl request config"
```

---

## Task 4: next.config.ts — wire up next-intl plugin

**Files:**
- Modify: `next.config.ts`

- [ ] **Step 1: Update next.config.ts**

Replace the full contents of `next.config.ts`:

```ts
import type { NextConfig } from 'next'
import createNextIntlPlugin from 'next-intl/plugin'

const withNextIntl = createNextIntlPlugin('./i18n/request.ts')

const nextConfig: NextConfig = {}

export default withNextIntl(nextConfig)
```

- [ ] **Step 2: Verify build compiles**

```bash
npx next build 2>&1 | head -20
```

Expected: no import errors (build may fail on missing pages — that's OK at this stage).

- [ ] **Step 3: Commit**

```bash
git add next.config.ts
git commit -m "feat: wire next-intl plugin into next.config.ts"
```

---

## Task 5: Translation message files

**Files:**
- Create: `messages/tr.json`
- Create: `messages/en.json`
- Create: `messages/de.json`
- Create: `messages/ar.json`

- [ ] **Step 1: Create Turkish messages (source of truth)**

```bash
mkdir -p messages
```

Create `messages/tr.json`:

```json
{
  "nav": {
    "home": "Ana Sayfa",
    "collections": "Koleksiyonlar",
    "search": "Ara",
    "cart": "Sepet",
    "account": "Hesabım"
  },
  "product": {
    "addToCart": "Sepete Ekle",
    "outOfStock": "Stokta Yok",
    "price": "Fiyat",
    "variants": "Seçenekler",
    "relatedProducts": "Benzer Ürünler"
  },
  "cart": {
    "empty": "Sepetiniz boş",
    "total": "Toplam",
    "subtotal": "Ara toplam",
    "checkout": "Ödemeye Geç",
    "remove": "Kaldır",
    "quantity": "Adet",
    "continueShopping": "Alışverişe Devam Et"
  },
  "account": {
    "login": "Giriş Yap",
    "register": "Kayıt Ol",
    "logout": "Çıkış Yap",
    "orders": "Siparişlerim",
    "email": "E-posta",
    "password": "Şifre",
    "firstName": "Ad",
    "lastName": "Soyad",
    "noOrders": "Henüz siparişiniz yok"
  },
  "search": {
    "placeholder": "Ürün ara...",
    "noResults": "Sonuç bulunamadı",
    "results": "{count} ürün bulundu"
  },
  "common": {
    "loading": "Yükleniyor...",
    "error": "Bir hata oluştu",
    "retry": "Tekrar Dene",
    "close": "Kapat",
    "save": "Kaydet"
  }
}
```

- [ ] **Step 2: Create English messages**

Create `messages/en.json`:

```json
{
  "nav": {
    "home": "Home",
    "collections": "Collections",
    "search": "Search",
    "cart": "Cart",
    "account": "My Account"
  },
  "product": {
    "addToCart": "Add to Cart",
    "outOfStock": "Out of Stock",
    "price": "Price",
    "variants": "Options",
    "relatedProducts": "Related Products"
  },
  "cart": {
    "empty": "Your cart is empty",
    "total": "Total",
    "subtotal": "Subtotal",
    "checkout": "Checkout",
    "remove": "Remove",
    "quantity": "Quantity",
    "continueShopping": "Continue Shopping"
  },
  "account": {
    "login": "Log In",
    "register": "Register",
    "logout": "Log Out",
    "orders": "My Orders",
    "email": "Email",
    "password": "Password",
    "firstName": "First Name",
    "lastName": "Last Name",
    "noOrders": "You have no orders yet"
  },
  "search": {
    "placeholder": "Search products...",
    "noResults": "No results found",
    "results": "{count} products found"
  },
  "common": {
    "loading": "Loading...",
    "error": "Something went wrong",
    "retry": "Try Again",
    "close": "Close",
    "save": "Save"
  }
}
```

- [ ] **Step 3: Create German messages**

Create `messages/de.json`:

```json
{
  "nav": {
    "home": "Startseite",
    "collections": "Kollektionen",
    "search": "Suche",
    "cart": "Warenkorb",
    "account": "Mein Konto"
  },
  "product": {
    "addToCart": "In den Warenkorb",
    "outOfStock": "Nicht vorrätig",
    "price": "Preis",
    "variants": "Optionen",
    "relatedProducts": "Ähnliche Produkte"
  },
  "cart": {
    "empty": "Ihr Warenkorb ist leer",
    "total": "Gesamt",
    "subtotal": "Zwischensumme",
    "checkout": "Zur Kasse",
    "remove": "Entfernen",
    "quantity": "Menge",
    "continueShopping": "Weiter einkaufen"
  },
  "account": {
    "login": "Anmelden",
    "register": "Registrieren",
    "logout": "Abmelden",
    "orders": "Meine Bestellungen",
    "email": "E-Mail",
    "password": "Passwort",
    "firstName": "Vorname",
    "lastName": "Nachname",
    "noOrders": "Sie haben noch keine Bestellungen"
  },
  "search": {
    "placeholder": "Produkte suchen...",
    "noResults": "Keine Ergebnisse gefunden",
    "results": "{count} Produkte gefunden"
  },
  "common": {
    "loading": "Lädt...",
    "error": "Etwas ist schiefgelaufen",
    "retry": "Erneut versuchen",
    "close": "Schließen",
    "save": "Speichern"
  }
}
```

- [ ] **Step 4: Create Arabic messages**

Create `messages/ar.json`:

```json
{
  "nav": {
    "home": "الرئيسية",
    "collections": "المجموعات",
    "search": "بحث",
    "cart": "السلة",
    "account": "حسابي"
  },
  "product": {
    "addToCart": "أضف إلى السلة",
    "outOfStock": "غير متوفر",
    "price": "السعر",
    "variants": "الخيارات",
    "relatedProducts": "منتجات مشابهة"
  },
  "cart": {
    "empty": "سلتك فارغة",
    "total": "الإجمالي",
    "subtotal": "المجموع الفرعي",
    "checkout": "إتمام الشراء",
    "remove": "إزالة",
    "quantity": "الكمية",
    "continueShopping": "مواصلة التسوق"
  },
  "account": {
    "login": "تسجيل الدخول",
    "register": "إنشاء حساب",
    "logout": "تسجيل الخروج",
    "orders": "طلباتي",
    "email": "البريد الإلكتروني",
    "password": "كلمة المرور",
    "firstName": "الاسم الأول",
    "lastName": "اسم العائلة",
    "noOrders": "ليس لديك طلبات بعد"
  },
  "search": {
    "placeholder": "ابحث عن المنتجات...",
    "noResults": "لم يتم العثور على نتائج",
    "results": "تم العثور على {count} منتج"
  },
  "common": {
    "loading": "جارٍ التحميل...",
    "error": "حدث خطأ ما",
    "retry": "حاول مجددًا",
    "close": "إغلاق",
    "save": "حفظ"
  }
}
```

- [ ] **Step 5: Commit**

```bash
git add messages/
git commit -m "feat: add translation messages for TR, EN, DE, AR"
```

---

## Task 6: proxy.ts — locale detection and redirect

**Files:**
- Create: `proxy.ts`

- [ ] **Step 1: Install locale matching library**

```bash
npm install negotiator @formatjs/intl-localematcher
npm install -D @types/negotiator
```

- [ ] **Step 2: Create proxy.ts**

Create `proxy.ts` in the project root:

```ts
import { NextResponse } from 'next/server'
import type { NextRequest } from 'next/server'
import Negotiator from 'negotiator'
import { match } from '@formatjs/intl-localematcher'
import { locales, defaultLocale } from './i18n/config'

function getLocale(request: NextRequest): string {
  const acceptLanguage = request.headers.get('accept-language') ?? ''
  const headers = { 'accept-language': acceptLanguage }
  const languages = new Negotiator({ headers }).languages()
  try {
    return match(languages, [...locales], defaultLocale)
  } catch {
    return defaultLocale
  }
}

export function proxy(request: NextRequest) {
  const { pathname } = request.nextUrl

  const pathnameHasLocale = locales.some(
    (locale) => pathname.startsWith(`/${locale}/`) || pathname === `/${locale}`
  )

  if (pathnameHasLocale) return NextResponse.next()

  const locale = getLocale(request)
  request.nextUrl.pathname = `/${locale}${pathname}`
  return NextResponse.redirect(request.nextUrl)
}

export const config = {
  matcher: [
    '/((?!_next/static|_next/image|favicon.ico|sitemap.xml|robots.txt).*)',
  ],
}
```

- [ ] **Step 3: Verify TypeScript**

```bash
npx tsc --noEmit
```

Expected: no errors.

- [ ] **Step 4: Commit**

```bash
git add proxy.ts package.json package-lock.json
git commit -m "feat: add proxy.ts for locale detection and redirect"
```

---

## Task 7: Migrate app/ to app/[locale]/

**Files:**
- Create: `app/[locale]/layout.tsx` (replaces `app/layout.tsx`)
- Create: `app/[locale]/page.tsx` (replaces `app/page.tsx`)
- Modify: `app/layout.tsx` → delete after migration

- [ ] **Step 1: Read current app/layout.tsx**

```bash
cat app/layout.tsx
```

- [ ] **Step 2: Create app/[locale]/ directory**

```bash
mkdir -p app/\[locale\]
```

- [ ] **Step 3: Create app/[locale]/layout.tsx**

Create `app/[locale]/layout.tsx`:

```tsx
import { NextIntlClientProvider } from 'next-intl'
import { getMessages } from 'next-intl/server'
import { notFound } from 'next/navigation'
import { locales, isRtl, type Locale } from '@/i18n/config'
import '../globals.css'

export function generateStaticParams() {
  return locales.map((locale) => ({ locale }))
}

export default async function LocaleLayout({
  children,
  params,
}: LayoutProps<'/[locale]'>) {
  const { locale } = await params

  if (!locales.includes(locale as Locale)) notFound()

  const messages = await getMessages()
  const dir = isRtl(locale as Locale) ? 'rtl' : 'ltr'

  return (
    <html lang={locale} dir={dir}>
      <body>
        <NextIntlClientProvider messages={messages}>
          {children}
        </NextIntlClientProvider>
      </body>
    </html>
  )
}
```

- [ ] **Step 4: Create app/[locale]/page.tsx**

Create `app/[locale]/page.tsx`:

```tsx
import { getTranslations } from 'next-intl/server'

export default async function HomePage() {
  const t = await getTranslations('nav')
  return (
    <main>
      <h1>{t('home')}</h1>
    </main>
  )
}
```

- [ ] **Step 5: Delete old root layout and page**

```bash
rm app/layout.tsx app/page.tsx
```

- [ ] **Step 6: Move globals.css reference**

The `globals.css` is already imported in `app/[locale]/layout.tsx` via `../globals.css`. Verify the path resolves correctly:

```bash
ls app/globals.css
```

Expected: file exists.

- [ ] **Step 7: Verify dev server starts**

```bash
npm run dev &
sleep 5
curl -s -o /dev/null -w "%{http_code}" http://localhost:3000/
```

Expected: `307` (redirect to `/tr/`).

```bash
curl -s -o /dev/null -w "%{http_code}" http://localhost:3000/tr
```

Expected: `200`.

Kill the dev server: `kill %1`

- [ ] **Step 8: Commit**

```bash
git add app/ 
git commit -m "feat: migrate app/ to app/[locale]/ with next-intl layout"
```

---

## Task 8: Add @inContext support to shopifyFetch

**Files:**
- Modify: `lib/shopify/types.ts`
- Modify: `lib/shopify/client.ts`
- Modify: `lib/shopify/client.test.ts`

- [ ] **Step 1: Write failing test for @inContext**

Add this test to `lib/shopify/client.test.ts` inside the existing `describe('shopifyFetch')` block:

```ts
it('injects @inContext directive when locale is provided', async () => {
  const mockFetch = vi.mocked(fetch)
  mockFetch.mockResolvedValueOnce(
    new Response(JSON.stringify({ data: { test: true } }), { status: 200 })
  )

  await shopifyFetch({ query: 'query GetProduct { product { title } }', locale: 'de' })

  const body = JSON.parse((mockFetch.mock.calls[0][1] as RequestInit).body as string)
  expect(body.query).toContain('@inContext(language: DE, country: DE)')
})
```

- [ ] **Step 2: Run test to verify it fails**

```bash
npx vitest run lib/shopify/client.test.ts
```

Expected: FAIL — `locale` property does not exist on `ShopifyFetchOptions`.

- [ ] **Step 3: Update ShopifyFetchOptions type**

In `lib/shopify/types.ts`, replace the `ShopifyFetchOptions` type:

```ts
export type ShopifyFetchOptions = {
  query: string
  variables?: Record<string, unknown>
  cache?: RequestCache
  revalidate?: number
  locale?: string
}
```

- [ ] **Step 4: Update shopifyFetch to inject @inContext**

In `lib/shopify/client.ts`, replace the full file contents:

```ts
import type { ShopifyFetchOptions, ShopifyResponse } from './types'
import { shopifyLocaleMap, defaultLocale, type Locale } from '../../i18n/config'

const ENDPOINT = `https://${process.env.SHOPIFY_STORE_DOMAIN}/api/2025-01/graphql.json`
const TOKEN = process.env.SHOPIFY_STOREFRONT_ACCESS_TOKEN!

function injectContext(query: string, locale: string): string {
  const mapping = shopifyLocaleMap[locale as Locale] ?? shopifyLocaleMap[defaultLocale]
  const context = `@inContext(language: ${mapping.language}, country: ${mapping.country})`

  // Insert @inContext after the first query/mutation keyword and its name+args
  return query.replace(
    /^(\s*(?:query|mutation)\s+\w*\s*(?:\([^)]*\))?)/m,
    `$1 ${context}`
  )
}

export async function shopifyFetch<T>({
  query,
  variables,
  cache,
  revalidate,
  locale,
}: ShopifyFetchOptions): Promise<T> {
  const finalQuery = locale ? injectContext(query, locale) : query

  const res = await fetch(ENDPOINT, {
    method: 'POST',
    headers: {
      'Content-Type': 'application/json',
      'X-Shopify-Storefront-Access-Token': TOKEN,
    },
    body: JSON.stringify({ query: finalQuery, variables }),
    ...(cache ? { cache } : {}),
    ...(revalidate !== undefined ? { next: { revalidate } } : {}),
  })

  const json: ShopifyResponse<T> = await res.json()

  if (json.errors?.length) {
    throw new Error(json.errors.map((e) => e.message).join(', '))
  }

  return json.data
}
```

- [ ] **Step 5: Run all tests to verify they pass**

```bash
npx vitest run
```

Expected: all tests pass (including the new @inContext test).

- [ ] **Step 6: Verify TypeScript**

```bash
npx tsc --noEmit
```

Expected: no errors.

- [ ] **Step 7: Commit**

```bash
git add lib/shopify/types.ts lib/shopify/client.ts lib/shopify/client.test.ts
git commit -m "feat: add @inContext locale support to shopifyFetch"
```

---

## Task 9: Update Shopify query functions to accept locale

**Files:**
- Modify: `lib/shopify/queries/products.ts`
- Modify: `lib/shopify/queries/collections.ts`
- Modify: `lib/shopify/queries/cart.ts`
- Modify: `lib/shopify/queries/search.ts`

- [ ] **Step 1: Update products.ts**

In `lib/shopify/queries/products.ts`, add `locale?: string` to each function signature and pass it to `shopifyFetch`. Replace the full file:

```ts
import { shopifyFetch } from '../client'
import type { ShopifyProduct, PageInfo } from '../types'

const PRODUCT_FIELDS = `
  id handle title description descriptionHtml
  availableForSale tags
  featuredImage { url altText width height }
  images(first: 10) { nodes { url altText width height } }
  priceRange {
    minVariantPrice { amount currencyCode }
    maxVariantPrice { amount currencyCode }
  }
  options { id name values }
  variants(first: 100) {
    nodes {
      id title availableForSale
      selectedOptions { name value }
      price { amount currencyCode }
      compareAtPrice { amount currencyCode }
    }
  }
`

export async function getProduct(handle: string, locale?: string): Promise<ShopifyProduct | null> {
  const data = await shopifyFetch<{ product: ShopifyProduct | null }>({
    query: `
      query GetProduct($handle: String!) {
        product(handle: $handle) { ${PRODUCT_FIELDS} }
      }
    `,
    variables: { handle },
    revalidate: 3600,
    locale,
  })
  return data.product
}

export async function getProducts(first = 12, locale?: string): Promise<ShopifyProduct[]> {
  const data = await shopifyFetch<{ products: { nodes: ShopifyProduct[] } }>({
    query: `
      query GetProducts($first: Int!) {
        products(first: $first, sortKey: BEST_SELLING) {
          nodes { ${PRODUCT_FIELDS} }
        }
      }
    `,
    variables: { first },
    revalidate: 3600,
    locale,
  })
  return data.products.nodes
}

export async function getProductsByCollection(
  handle: string,
  first = 24,
  after?: string,
  locale?: string
): Promise<{ products: ShopifyProduct[]; pageInfo: PageInfo }> {
  const data = await shopifyFetch<{
    collection: { products: { nodes: ShopifyProduct[]; pageInfo: PageInfo } } | null
  }>({
    query: `
      query GetCollectionProducts($handle: String!, $first: Int!, $after: String) {
        collection(handle: $handle) {
          products(first: $first, after: $after) {
            nodes { ${PRODUCT_FIELDS} }
            pageInfo { hasNextPage hasPreviousPage startCursor endCursor }
          }
        }
      }
    `,
    variables: { handle, first, after },
    revalidate: 3600,
    locale,
  })
  const col = data.collection
  if (!col) return { products: [], pageInfo: { hasNextPage: false, hasPreviousPage: false, startCursor: null, endCursor: null } }
  return { products: col.products.nodes, pageInfo: col.products.pageInfo }
}
```

- [ ] **Step 2: Update collections.ts**

Replace the full contents of `lib/shopify/queries/collections.ts`:

```ts
import { shopifyFetch } from '../client'
import type { ShopifyCollection } from '../types'

const COLLECTION_FIELDS = `
  id handle title description
  image { url altText width height }
`

export async function getCollections(first = 20, locale?: string): Promise<ShopifyCollection[]> {
  const data = await shopifyFetch<{ collections: { nodes: ShopifyCollection[] } }>({
    query: `
      query GetCollections($first: Int!) {
        collections(first: $first) {
          nodes { ${COLLECTION_FIELDS} }
        }
      }
    `,
    variables: { first },
    revalidate: 3600,
    locale,
  })
  return data.collections.nodes
}

export async function getCollection(handle: string, locale?: string): Promise<ShopifyCollection | null> {
  const data = await shopifyFetch<{ collection: ShopifyCollection | null }>({
    query: `
      query GetCollection($handle: String!) {
        collection(handle: $handle) { ${COLLECTION_FIELDS} products(first: 1) { nodes { id } pageInfo { hasNextPage hasPreviousPage startCursor endCursor } } }
      }
    `,
    variables: { handle },
    revalidate: 3600,
    locale,
  })
  return data.collection
}
```

- [ ] **Step 3: Update search.ts**

Replace the full contents of `lib/shopify/queries/search.ts`:

```ts
import { shopifyFetch } from '../client'
import type { ShopifyProduct, PageInfo } from '../types'

const PRODUCT_SUMMARY = `
  id handle title availableForSale
  featuredImage { url altText width height }
  priceRange { minVariantPrice { amount currencyCode } }
`

export async function searchProducts(
  query: string,
  first = 24,
  after?: string,
  locale?: string
): Promise<{ products: ShopifyProduct[]; pageInfo: PageInfo }> {
  const data = await shopifyFetch<{
    search: { nodes: ShopifyProduct[]; pageInfo: PageInfo }
  }>({
    query: `
      query SearchProducts($query: String!, $first: Int!, $after: String) {
        search(query: $query, first: $first, after: $after, types: PRODUCT) {
          nodes { ... on Product { ${PRODUCT_SUMMARY} } }
          pageInfo { hasNextPage hasPreviousPage startCursor endCursor }
        }
      }
    `,
    variables: { query, first, after },
    cache: 'no-store',
    locale,
  })
  return { products: data.search.nodes, pageInfo: data.search.pageInfo }
}
```

- [ ] **Step 4: Run all tests**

```bash
npx vitest run
```

Expected: all tests pass.

- [ ] **Step 5: Verify TypeScript**

```bash
npx tsc --noEmit
```

Expected: no errors.

- [ ] **Step 6: Commit**

```bash
git add lib/shopify/queries/
git commit -m "feat: add locale param to all Shopify query functions"
```

---

## Task 10: Verify end-to-end locale routing

**Files:** None created — smoke test only.

- [ ] **Step 1: Start dev server**

```bash
npm run dev &
sleep 5
```

- [ ] **Step 2: Verify redirect from root**

```bash
curl -s -o /dev/null -w "%{http_code} %{redirect_url}\n" http://localhost:3000/
```

Expected: `307 http://localhost:3000/tr/` (or `tr` based on your system locale).

- [ ] **Step 3: Verify locale pages respond**

```bash
for locale in tr en de ar; do
  code=$(curl -s -o /dev/null -w "%{http_code}" http://localhost:3000/$locale)
  echo "$locale: $code"
done
```

Expected: all return `200`.

- [ ] **Step 4: Verify RTL for Arabic**

```bash
curl -s http://localhost:3000/ar | grep -o 'dir="[^"]*"'
```

Expected: `dir="rtl"`.

- [ ] **Step 5: Kill dev server and commit**

```bash
kill %1
git commit --allow-empty -m "feat: i18n complete — TR/EN/DE/AR with RTL and @inContext"
```
